services:
  gmail-sorter:
    build: .
    container_name: gmail-sorter

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./data:/app/data
    
    networks:
      - ai_platform

networks:
  ai_platform:
    external: true